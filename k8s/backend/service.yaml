apiVersion: v1
kind: Service

metadata:

  name: backend

  namespace: mortgagebank

spec:

  selector:

    app: mortgage-backend

  ports:

    - protocol: TCP

      port: 8080

      targetPort: 8080

  type: ClusterIP